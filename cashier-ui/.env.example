# Backend API base URL.
# In development, leave this empty — the Vite dev server proxies /api/* to
# http://localhost:8000 automatically (see vite.config.ts).
# In production, set this to the full backend origin, e.g.:
# VITE_API_BASE_URL=https://api.yourapp.com
VITE_API_BASE_URL=
