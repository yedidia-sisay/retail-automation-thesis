from django.urls import path

# Sprint 7 uses a session-based checkout endpoint for weighted items.
# This urls.py is intentionally empty for now, but kept so the app has
# a conventional structure if/when weighted-item endpoints are split out.

urlpatterns: list = []
